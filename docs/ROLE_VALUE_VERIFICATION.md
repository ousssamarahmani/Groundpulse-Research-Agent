# Role-specific Workspace verification

The running Workspace exposes a New research intake with a Research role selector and role-specific scenario catalog.

For **CubeSat analyst**, the tested default scenario is “Orbit readiness check,” with an expected output covering orbit freshness, derived period, planning inputs, and a safe-to-use boundary.

For **Satellite engineer**, switching the role updates the scenario catalog to “Subsystem evidence map” and “Ground-link readiness.” The tested subsystem scenario updates the title, question, and expected output to an ADCS/EPS/communications/thermal/operations evidence matrix with engineering gaps and next evidence tasks.

The live source session simultaneously rendered ISS (ZARYA), NORAD 25544, its published epoch, mean motion, a derived period, and an explicit operational telemetry gap. This confirms that role framing and source-backed evidence are visible in the same workflow rather than being disconnected marketing copy.
